### 1. 样条拟合（Spline Fitting）

**适用场景**：数据趋势复杂（如多峰、多谷），且希望拟合曲线**平滑且避免高次多项式震荡**（龙格现象），常见于工程绘图、数据插值。**核心原理**：将数据区间分成多个 “子区间”，每个子区间用低次多项式（通常是三次多项式，即 “三次样条”）拟合，同时保证 “子区间连接处的一阶、二阶导数连续”（曲线平滑无断点）。

- 平滑性好，无高次多项式的边缘震荡；
- 可通过 “节点数量” 控制灵活性（节点越多，拟合越精细）