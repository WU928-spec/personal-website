`rm(list = ls())`
`data <- read.csv("D:/data/data/Score Analysis/student_data.csv")`
`colSums(is.na(data))`
`n=200`
`data$study_hour[sample(1:n, 8)] <- NA`
`write.csv(data,` 
          `file = "D:/data/data/Score Analysis/student_data.csv",` 
          `row.names = FALSE)`
`data$sleep_hour[sample(1:n, 10)] <- NA` 
`write.csv(data,` 
          `file = "D:/data/data/Score Analysis/student_data.csv",` 
          `row.names = FALSE)`
`missing <- data.frame(`
  `缺失数量 = colSums(is.na(data)),`
  `缺失百分比 = round(colMeans(is.na(data)) * 100, 2))`
